<view class="login-page {{!animationEnabled ? 'reduce-motion' : ''}}">
  <!-- 背景扫描线 -->
  <view class="scanline"></view>

  <!-- Logo 区域 -->
  <view class="brand">
    <view class="logo-mark">
      <view class="logo-ring">
        <view class="logo-inner">
          <view class="logo-s">
            <view class="s-top"></view>
            <view class="s-bottom"></view>
          </view>
        </view>
      </view>
      <view class="logo-glow"></view>
    </view>

    <text class="terminal-title">终端接入</text>

    <view class="terminal-divider">
      <view class="div-line"></view>
      <text class="div-label">脉冲 · 编队 · 指令 · 镜像</text>
      <view class="div-line"></view>
    </view>

    <view class="terminal-tags">
      <text class="tag-item">多人编队</text>
      <text class="tag-sep">|</text>
      <text class="tag-item">人格镜像</text>
      <text class="tag-sep">|</text>
      <text class="tag-item">导航核心</text>
    </view>
  </view>

  <!-- 系统状态 -->
  <view class="system-status">
    <view class="status-header">
      <view class="status-line"></view>
      <text class="status-label-cn">系统状态</text>
      <view class="status-line"></view>
    </view>

    <view class="status-grid">
      <view class="status-row">
        <view class="status-dot"></view>
        <text class="status-module">编队模块</text>
        <text class="status-value">在线</text>
      </view>
      <view class="status-row">
        <view class="status-dot"></view>
        <text class="status-module">镜像模块</text>
        <text class="status-value">在线</text>
      </view>
      <view class="status-row">
        <view class="status-dot"></view>
        <text class="status-module">指令投影</text>
        <text class="status-value">在线</text>
      </view>
    </view>
  </view>

  <!-- 终端接入 -->
  <view class="auth-section" wx:if="{{!connecting}}">
    <view class="auth-header">
      <view class="auth-line"></view>
      <text class="auth-label-cn">终端接入</text>
      <view class="auth-line"></view>
    </view>

    <button
      class="login-btn flight-primary {{loading ? 'is-loading' : ''}}"
      hover-class="login-btn--hover"
      hover-stay-time="100"
      bindtap="onLogin"
    >
      <text class="btn-text">接入终端</text>
      <view wx:if="{{loading}}" class="btn-loading">
        <view class="spinner"></view>
      </view>
    </button>

    <text class="auth-hint">首次接入后自动创建识别档案</text>
  </view>

  <!-- 接入动画 -->
  <view class="connecting-overlay" wx:if="{{connecting}}">
    <view class="connecting-box">
      <text class="connecting-title">正在接入</text>
      <view class="connecting-divider"></view>

      <view class="connecting-steps">
        <view class="step-row" wx:for="{{steps}}" wx:key="index">
          <text class="step-text">{{item.text}}</text>
          <text class="step-check" wx:if="{{item.done}}">[OK]</text>
        </view>
      </view>

      <view class="connecting-divider" wx:if="{{accessGranted}}"></view>
      <text class="access-granted" wx:if="{{accessGranted}}">已接入</text>
    </view>
  </view>

  <!-- 底部信息 -->
  <view class="bottom-info">
    <text class="build-info">版本 1.0.0</text>
  </view>
</view>
