.bi-card {
  border-radius: 20rpx;
  border: 1rpx solid rgba(0, 175, 255, 0.12);
  background: #0A0F18;
  padding: 28rpx 26rpx;
  margin-bottom: 20rpx;
}

.bi-header {
  display: flex;
  flex-direction: column;
}

.bi-cn {
  font-size: 26rpx;
  font-weight: 700;
  color: #00C8FF;
  letter-spacing: 2rpx;
}

.bi-en {
  font-family: 'SF Mono', 'Courier New', monospace;
  font-size: 17rpx;
  letter-spacing: 5rpx;
  color: rgba(255, 255, 255, 0.34);
  margin-top: 6rpx;
}

.bi-divider {
  /* 继承 app.wxss .sr-card-line */
}

.bi-blocks {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 16rpx;
  margin-top: 16rpx;
}

.bi-block {
  padding: 16rpx;
  background: rgba(255, 255, 255, 0.02);
  border: 1rpx solid rgba(255, 255, 255, 0.05);
  border-radius: 12rpx;
  display: flex;
  flex-direction: column;
  gap: 6rpx;
}

.bi-block-label {
  font-size: 20rpx;
  color: #7C8698;
}

.bi-block-value {
  font-size: 26rpx;
  font-weight: 600;
  color: #FFFFFF;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.bi-block-sub {
  font-size: 20rpx;
  color: #00AFFF;
}

.bi-block-num {
  font-size: 36rpx;
  font-weight: 700;
  color: #00AFFF;
  font-family: "SF Mono", "Menlo", "Courier New", monospace;
  font-variant-numeric: tabular-nums;
}

.bi-density-wrap {
  display: flex;
  align-items: center;
  gap: 12rpx;
}

.bi-density-bar {
  flex: 1;
  height: 8rpx;
  background: rgba(255, 255, 255, 0.06);
  border-radius: 4rpx;
  overflow: hidden;
}

.bi-density-fill {
  height: 100%;
  border-radius: 4rpx;
  transition: width 0.3s;
}

.bi-density-high { background: #36FF74; }
.bi-density-mid { background: #00AFFF; }
.bi-density-low { background: #FF4D4F; }

.bi-density-text {
  font-size: 18rpx;
  color: #7C8698;
  font-family: monospace;
  letter-spacing: 1rpx;
  flex-shrink: 0;
}

.reduce-motion .bi-density-fill { transition: none; }
