解决下面问题：
1.积分总览的界面中要禁止房间页面的滚动。
2.积分总览中在上方增加折线图，显示每个成员的积分和时间的变化
3.折线图中能够选择只显示哪些成员的。
4.对于轮次的数据需要保存，不应该影响到后续的计算
5.房主结束本轮后，所有数据都要清零，只保留一个历史记录，这个历史记录可以被查看。
6.积分总览中的数据有问题，负分没有显示出来，整体总分数应该为0
7.为了保证性能，应该有一个线程去计算这个总览图放置在redis中，而不是每个用户点击后都单独计算。
8.成员的图片没有在图片框中居中正确显示。

解决下面问题，页面设计严格调用skill：ui-ux-pro-max：
1.登陆页面不需要给用户填写头像和昵称，只有新用户第一次时在后台自动生成，后续用数据库中的名称和头像。
2.点开积分总览没有看见积分和时间的折线图。
3.座位模式和简易模式的切换应该在成员行有按钮切换
4.删除房间号行的拍照按钮，点击分享出现扫码图片，扫码能快速进房间，或者分享图片，以及分享链接，复制链接或者扫描二维码进入小程序都能直接进入房间
5.隐藏房间页的滚动条

**【任务背景】**
你现在是一个精通微信小程序原生开发与数据可视化的资深前端架构师。我们需要在现有的 HappyRecord 记分辅助小程序中，在积分总览页面中增加一个“积分-时间（Score vs. Time）”的折线图组件。

**【UI/UX 图表设计规范（绝对极简主义）】**
- **摒弃传统图表元素**：绝对不要显示 X/Y 坐标轴、网格线（Grid lines）、刻度线或繁杂的图例文字。图表必须完美融入暗黑模式背景。
- **线条与质感（核心发光视效）**：
    - 折线必须是平滑的贝塞尔曲线（Smooth Bezier Curve），不要生硬的折角。
    - 线条带有明显的 Neon/外发光效果。
    - 动态色彩映射：当积分为正时，线条和下方的渐变填充区域呈现科技蓝或荧光橙；当积分为负时，无缝过渡为明亮的警示红。
- **渐变填充**：曲线下方需附带顺滑的垂直线性渐变（Linear Gradient），从顶部的半透明主题色渐变至底部的全透明（类似毛玻璃溶液的通透感）。

**【技术栈与性能要求】**
- **渲染引擎**：必须使用微信小程序原生的 `Canvas 2D` API 进行绘制，确保在 iOS/Android 真机上的极限性能与抗锯齿效果。尽量不依赖庞大的第三方图表库（如 ECharts），以手写原生 Canvas 绘制逻辑优先，保持极小的代码包体积。
- **数据结构**：接收一个包含 `{ timestamp, score }` 的一维数组，自动计算极值并动态缩放 Canvas 内部的绘制比例（自适应 Y 轴边界）。
- **动画反馈**：组件挂载时，折线需具备“从左至右”顺滑生长的入场动画（通过 `requestAnimationFrame` 配合 Canvas 路径截取实现）。

**【交互细节】**
- **触摸交互（Touch Event）**：支持用户在图表区域横向长按/滑动，触发一个极简的垂直游标（一条带有透明度的细线），并在游标顶部悬浮显示该时间节点对应的具体数值（去除“分”单位，仅保留高对比度的数字）。

**【代码输出要求】**
1. **组件封装**：输出完整的自定义组件代码（`.json`, `.wxml`, `.wxss`, `.js`）。
2. **绘制逻辑**：在 JS 文件中详细提供 Canvas 2D 的绘制流程，包括获取 Context、坐标转换缩放（Data to Pixel）、贝塞尔曲线计算以及渐变色/阴影发光的 API 调用。
3. **性能防抖**：提供 `touchmove` 事件的节流（Throttle）或防抖逻辑，确保在滑动查看数据时 Canvas 重绘不会导致主线程卡顿。



**【任务背景】**
你现在是一个资深的微信小程序原生前端开发工程师。我需要对现有的“积分记录”页面进行重构。当前页面的 UI 是一个扁平的列表，每行显示“付款方 -> 分数 -> 收款方 -> 时间（如 06-02 19:26）”。整体设计风格为高端暗黑极简风。

**【UI 与交互变动需求】**

1. **新增“与我相关”过滤按钮**
  - 在页面顶部区域（“历史场次”按钮的左边），新增一个“我”的 Toggle 按钮。
  - **交互逻辑**：点击该按钮后，列表仅过滤并显示当前登录用户（无论是作为加分方还是扣分方）参与的积分记录。再次点击恢复显示全部记录。按钮需有明显的选中态（Active）和未选中态视觉区分。

2. **列表按“分钟”分组聚合 (Group by Minute)**
  - 消除现有列表项最右侧独立的“时间列”。
  - 将扁平的列表数据结构重组为按时间戳（精确到分钟，例如 `06-02 19:26`）分组的块状结构。】
  - 如果是当天的时间，不显示日期，只显示小时和分钟。
  - 为了增强页面性能，在redis中的数据要分页拉取，用户下滑时动态更新
  - **UI 呈现**：在同一分钟内发生的交易，使用同一个时间标题（Time Header）作为分组头部。时间标题需采用极简的暗色系次级文本颜色（如半透明灰），并支持吸顶效果（Sticky Header）。该时间标题下方紧跟该分钟内发生的所有记录行。

**【数据处理逻辑 (JS/TS 核心要求)】**

1. **数据转换算法**：
  - 原始数据可能是扁平数组 `[{ payer, payee, score, timestamp }, ...]`。
  - 必须提供一段清晰的 JS 逻辑，利用 `Array.reduce` 等高阶函数，将原始数据转换为分组结构 `[{ timeStr: '06-02 19:26', records: [...] }, ...]`。格式化时间时丢弃秒数。
2. **过滤逻辑**：
  - 提供基于当前用户 `myUserId` 的 `filter` 方法。只有当 `payerId === myUserId` 或 `payeeId === myUserId` 时，记录才被保留，并在过滤后再进行分组渲染。

**【代码输出要求】**
1. **WXML 结构**：使用嵌套的 `wx:for`（外层循环时间分组，内层循环该时间段内的记录）。
2. **WXSS 样式**：时间标题（Header）需实现 `position: sticky; top: 0;` 以提升浏览体验，背景色需与暗黑模式背景融为一体（或带有轻微的毛玻璃 `backdrop-filter` 以防止文字重叠）。确保原有的“出分方 -> 绿字/红字 -> 得分方”的极简排版在移除时间列后依然居中协调。
3. **JS 逻辑**：输出完整的数据重组（Grouping）和状态切换（Toggle Filter）代码块。

修复下面问题，前端修改调用skill：ui-ux-pro-max设计更改页面：
1.全局删除 底分 。
2.每个人只能有一个房间，首页中，如果已经创建了房间，创建房间变为账号的房间信息。
3.已有房间方便显示复制，打开二维码，分享。和房间里面的一致。二维码生成功能需要完善。
4.加入房间的布局和创建房间要一致，加入房间按钮居中和创建房间一致。扫描二维码换成扫一扫图标，放在加入房间右边角落。
5.我的里面头像昵称等布局存在问题。重新设计排版
6.账号一栏不需要显示微信授权，删除。
7.积分记录，历史房间，放在设置上面，头像栏下面，重新设置布局。点击后切换新页面，才刷新数据。
8.修复声音未播报的问题
9.退出登陆按钮文字未居中显示。
10.最后再次优化和调整首页和我的页面布局


修复下面问题，前端修改调用skill：ui-ux-pro-max设计更改页面：
1.加入房间时，用户第一时间只看到了自己一个人（房主肯定是在的），需要修复数据刷新。
2.输入积分的键盘中，删除 添加留证。
3.积分记录的历史场次只有房主可以看到，点开后，显示房间内的轮次记录，轮次记录显示该轮次所有的用户名称和头像，
开始和结束时间。点开一条记录后公用积分总览页面的数据渲染。保证积分总览页面的全局唯一（性能需要）。
同时每个用户在自己的历史记录中能查看到有他在的场次，点击后同样调出积分总览界面。积分总览界面保证全局单例。
其他性能方便的保障你来补充


你现在是一位负责系统重构的资深 Java 架构师。目前部署的物理服务器资源极度受限（仅有 2核 2G 内存，3Mbps 带宽），我需要你帮我对现有代码和基建进行“极限瘦身与平滑替换”。

【现有架构背景】
- 后端：Java 17 + Spring Boot 3.2.5 + MyBatis-Plus 3.5.6 + MySQL 8.0 + Redis 7 (Redisson) + MinIO 8.5.9
- 部署：docker-compose 一键部署。
- 核心机制：WebSocket 房间内推送、MinIO 前端直传图片、Redis 存实时排行与锁、异步 MySQL 落盘。

【重构改造任务】
请帮我逐步产出以下四个方面的重构代码和配置文件：

任务一：全面剔除 MinIO，无缝接入阿里云 OSS
由于 MinIO 内存占用过高，且我已经购买了阿里云 OSS 容量包。
1. 请帮我修改 `pom.xml`，移除 MinIO 依赖，引入 `aliyun-sdk-oss`。
2. 请重写 `StorageController` 及对应的 Service 层逻辑。实现：后端读取配置（Endpoint, BucketName, AK, SK），为前端颁发用于阿里云 OSS 直传的预签名 URL（Presigned URL），确保依然是不经过后端中转的“前端直传”模式。

任务二：数据库与缓存的极限防宕机配置 (docker-compose.yml)
请帮我重写 `docker-compose.yml` 文件。要求：
1. **彻底删除 MinIO 相关的服务块**。
2. **MySQL 降级为 5.7**（以节省内存），必须通过 command 或挂载 my.cnf 的方式，严格将 `innodb_buffer_pool_size` 限制为 128M，并设置 `character-set-server=utf8mb4`。
3. **Redis 限制内存**：必须在配置中增加 `--maxmemory 64mb --maxmemory-policy allkeys-lru`，防止排行榜或锁数据无限增长撑爆内存。

任务三：WebSocket Session 纯内存化排查
我已经有 WebSocket 的房间级实时推送逻辑。请审查并指导我：
必须确保 WebSocket 的在线 Session 映射（如 `Map<String, Session>`）是纯粹存放在 JVM 内存（如 `ConcurrentHashMap`）中的，**绝对不要使用 Redis 的 Pub/Sub 来做实时消息的分发转存**，以降低网络 IO 和 Redis 内存压力。

任务四：MySQL 5.7 语法兼容性审查
请审查现有的 Mybatis-Plus Entity 实体类设计：
指导我如何排查和修改，以防使用了 MySQL 8.0 的保留字（如 rank, groups 等），确保后续在 5.7 版本的 Docker 容器中能顺利执行 DDL 建表。

请先给出“任务一”和“任务二”的具体代码和 `docker-compose.yml` 完整文件。

# 角色与架构背景
你是一位拥有大厂千万级日活经验的资深 Java 后端架构师。
我们正在重构这个记分小程序后端。系统采用 Spring Boot 3 + MySQL + Redis 架构，运行在 2核2G 的轻量级容器中。
**核心架构原则**：
1. **极致写削峰**：彻底废弃了 MySQL 中的实时转账明细表。打牌期间所有的分数变动和流转仅在 Redis 内存中完成，仅在“结算”时批量落盘 MySQL。
2. **Redis 为核心**：对于活跃房间，系统只认 Redis。如果 Redis 里没有，则拒绝操作。

# 核心任务
为了保证这套“纯内存流转”架构在生产环境下的绝对健壮性，请帮我编写以下 4 个核心高阶保障模块的 Java 代码。

# 模块一：宕机恢复与缓存重建 (Cache Recovery)
**场景**：如果 Redis 重启导致活跃数据丢失，业务将瘫痪。
**需求**：
编写 `CacheWarmUpRunner` 类实现 `ApplicationRunner`。
在 Spring Boot 启动后，自动查询 MySQL 的 `session` 表中所有 `status = 0`（进行中）的场次。
将这些“未完结”的对局状态、成员名单、房间路由映射关系重新初始化到 Redis 中，无缝恢复状态。

# 模块二：幽灵对局兜底结算 (Ghost Session Sweeper)
**场景**：用户打完牌忘记点击“结束”，导致内存泄漏且数据未归档。
**需求**：
编写 `SessionSweeperTask` 定时任务类（`@Scheduled`）。
每隔 1 小时执行一次，扫描 Redis/MySQL 中“最后活跃时间超过 12 小时”且未结算的活跃场次。
调用现有的结算逻辑（`SessionService.settleSession`），强制结束对局，释放 Redis 内存并落盘归档。

# 模块三：基于 Lua 的纯 Redis 高并发转账 (Atomic Transfer)
**场景**：在彻底删除转账明细表后，使用纯 Redis 处理转账，需防范两人同时互相转账导致的并发覆盖。
**需求**：
在 `TransferService` 中使用 `StringRedisTemplate.execute()` 配合 **Redis Lua 脚本** 保证绝对原子性。
涉及两个 Redis Key：
1. **总分 Hash**：`mj:room:scores:{roomId}` (field 为 userId，value 为当前分数，允许为负)。
2. **流水 List**：`mj:room:events:{roomId}`。
   Lua 脚本需在一次执行中完成：
1. `HINCRBY` 扣减转出人 (`fromUserId`) 分数。
2. `HINCRBY` 增加接收人 (`toUserId`) 分数。
3. `RPUSH` 将完整动作组装成 JSON 字符串（如 `{"from": 111, "to": 222, "amount": 50, "time": 1717382400}`）压入流水 List，供最终结算时落盘使用。

# 模块四：优雅降级与全局异常拦截 (Graceful Degradation)
**场景**：Redis 重启的几秒钟内，底层会疯狂报错，不能将堆栈暴露给前端。
**需求**：
编写 `GlobalExceptionHandler` 类（`@RestControllerAdvice`）。
精确捕获 Redis 底层异常（如 `RedisConnectionFailureException`, `QueryTimeoutException` 等）。
记录 ERROR 级极简日志，并统一返回给前端友好的 JSON 响应：HTTP 状态码为 200，内部 `code` 设为 503，`msg` 设为："服务器正在进行极速快照同步，请稍等片刻后再试~"。

# 输出要求
请直接输出上述 4 个模块的高质量 Java 代码，包含必要的 `@Slf4j` 日志、异常捕获以及清晰的中文注释。代码需遵循阿里巴巴开发规范，力求精简。


# 角色与任务
你是一个顶级的 UI/UX 视觉设计师和微信小程序开发专家。
请帮我重构微信小程序的“房间详情工作台 (Room Board)”页面，我的页面部分，以及配套的“底部自定义数字键盘 (Custom Keypad)”。

# 核心设计语言与全局规范 (必须严格遵守)
1. **绝对封杀彩色 Emoji**：全页面（含按钮、空状态等）禁止使用任何形式的彩色 Emoji！所有图标必须替换为极简的纯白色/高亮色线框 SVG (Line Icons)。
2. **整体风格**：极致极简主义 (Extreme Minimalism)、暗黑模式 (Dark Mode)、毛玻璃质感 (Glassmorphism)。
3. **全局色彩与质感**：
    - 页面底色：`#0A0A0A`。
    - 卡片与面板底色：`rgba(255, 255, 255, 0.04)`。
    - 毛玻璃特效：`backdrop-filter: blur(20px)`。
    - 边缘高光：`border: 1px solid rgba(255, 255, 255, 0.08)`。

# 页面区块重构细节 (Pixel-pushing)

**1. Top Card: 房间信息控制台**
- **房间号**：“房间号”字样使用暗灰色 `rgba(255,255,255,0.4)`。后面的真实房间号（如 GJ5JEM）使用高亮蓝 `#0A84FF`，增加字间距 `letter-spacing: 4rpx`，使用等宽字体 (Monospace) 凸显密钥感。
- **操作栏图标**：分享、播报、解散等操作，替换为纯白色线框 Icon，默认透明度 `0.6`，按下 `0.3`。

**2. Middle Card: 玩家成员网格 (Grid)**
- **Header**：右侧的“座位”按钮改为极窄的幽灵按钮（边框 `1px solid rgba(255,255,255,0.1)`，配极简 Icon）。
- **玩家卡片 (交互核心)**：
    - 头像适当放大，外围增加微弱边框或光晕。
    - 选中状态（被点击操作时）：外围高亮，显示 `1px solid rgba(10, 132, 255, 0.5)`。
    - 玩家分数：数字字号显著放大，加粗，使用纯白无衬线字体，做到极其醒目（正负数可用颜色区分）。

**3. Bottom Card: 积分流水区**
- **操作按钮**：“我”和“历史场次”改为极简胶囊按钮（深灰背景 `rgba(255,255,255,0.08)`，纯白小字）。
- **空状态**：使用极简透明线框 SVG（如收纳盒/文档），透明度压到 `0.2`。文字“暂无记录”透明度 `0.3`。

**4. 底部弹窗：自定义数字键盘 (Custom Keypad)**
- **容器质感**：背景使用更暗的毛玻璃 `rgba(20, 20, 20, 0.85)`，配合 `blur(20px)`，顶部大圆角。
- **显数区 (Display Area)**：
    - 顶部目标名称（如“全求人哪吒”）使用 `rgba(255,255,255,0.5)`。
    - **输入数字必须巨大且震撼**（`font-size: 88rpx; font-weight: 600;`，纯白）。右侧“积分”二字缩小并底部对齐。
    - 下方加极细分割线 `border-bottom: 1px solid rgba(255, 255, 255, 0.05)`。
- **按键网格 (Keypad Grid)**：
    - 弃用实色背景！默认状态为 `rgba(255, 255, 255, 0.04)`，无边框，`16rpx` 圆角。数字字体放大、纯白。
    - 按下状态 (Hover)：背景变为 `rgba(255, 255, 255, 0.15)`，保证丝滑反馈。
    - 辅助键（`C` 和退格）：透明度压低到 `0.4`。
- **确认按钮**：高度加大，颜色使用 `#0A84FF`，增加发光阴影 `box-shadow: 0 8rpx 24rpx rgba(10, 132, 255, 0.25)`。

# 输出要求
请直接给出精细调校后的完整 `index.wxml` 和 `index.wxss` 代码。确保 CSS Grid 布局的 gap 优雅，组件结构清晰。

# 角色设定
你是一个精通微信原生小程序、CSS3 硬件加速动效，同时精通高并发 Java 后端架构（Spring Boot 体系）、大模型（LLM）API 对接的全栈架构师。深谙 Apple 级“极致极简主义（Extreme Minimalism）”与赛博朋克电竞视觉风格。

# 任务目标
为桌游/棋牌类小程序开发一个名为“每日状态监测（赛博运势）”的核心模块，小程序新增导航栏： 运势。放在房间右侧，导航栏第二个位置
该模块包含极具科技感的 3D 悬浮 UI，且后端必须实现【实时大模型 + 本地画像标签兜底】的高可用双引擎架构。在保证千人千面个性化的同时，严格控制 API 延迟带来的体验降级。

# 核心开发指令（请严格遵循）

## 1. 前端视觉与交互：赛博玄学面板 (UI/UX Design)
彻底抛弃传统黄历视觉，打造一个“高维状态监测面板”。
* **全局布局**：纯黑背景 `#0A0A0A`。
* **核心视觉 (3D 全息卡片)**：
    * 页面中央悬浮一张深色毛玻璃卡片（`backdrop-filter: blur(40px); background: rgba(255,255,255,0.05); border: 1px solid rgba(255,255,255,0.15)`）。
    * 包含内部发光感（光球颜色由后端返回，如 `#0A84FF`）。
    * 动效：利用纯 CSS `@keyframes` 或微信小程序 `onAccelerometerChange`（重力感应），实现卡片在 3D 空间内的 `rotateX` 和 `rotateY` 极其平滑的悬浮呼吸感。
* **数据区**：卡片下方陈列数据，全大写等宽字体（Monospace）。渲染【系统增益 (Buff)】和【风险警告 (Debuff)】字段。全靠纯文本与留白排版，禁用彩色 Icon。

## 2. 前端动效：赛博朋克加载障眼法 (UX Masking)
在发起 `wx.request` 到接口返回结果期间，必须有一个全屏的极简加载 UI，用于掩盖后端 API 的延迟。
* 摒弃传统的 Loading 菊花图。
* 使用等宽字体，通过 JS 定时器动态切换以下文案：
  `"正在连接神经元节点..." -> "正在解析历史磁场波动..." -> "量子塌缩完成，正在生成日签..."`
* 配合 CSS `@keyframes` 的文字微弱闪烁（flicker）特效。

## 3. 后端架构：历史画像标签化 (User Profiling)
在请求大模型之前，首先根据用户的积分数据（如近 10 场净积分 `netScore`），为其打上特征标签，枚举如下：
* `HIGH_RISK` (大输大赢型)
* `LOSING_STREAK` (近期连败/低迷)
* `WINNING_STREAK` (近期连胜/高昂)
* `STABLE` (稳健型)
  这个标签将用于注入大模型 Prompt，并且是后续触发兜底缓存的核心 Key。

## 4. 后端架构：超时熔断与双引擎编排 (CompletableFuture)
* **主引擎**：使用 `CompletableFuture.supplyAsync()` 包装大模型的 HTTP 调用，将历史数据标签作为 Prompt 变量注入，请求生成极简判词。
* **超时熔断**：使用 `orTimeout(2500, TimeUnit.MILLISECONDS)` 设置极其严格的 2.5 秒超时。
* **备用引擎**：使用 `exceptionally(ex -> fallbackFortune(userTag))` 实现兜底降级。
* **兜底方法 `fallbackFortune(userTag)`**：根据第一步算出的用户标签，从一个预先写好的本地静态 `Map<Tag, List<FortuneDTO>>`（或 Redis）中，极速抽取一条符合该画像的数据直接返回。确保即使 API 挂了，用户依然能得到贴合近期状态的结果。

# 输出要求
请直接输出生产级代码：
1. **小程序端 WXML & WXSS**：重点展示 3D 全息毛玻璃卡片的 CSS 硬件加速样式，以及文字微弱闪烁（flicker）特效。
2. **小程序端 JS**：重点展示“动态切换科技感加载文案”与接口请求的衔接逻辑。
3. **Java 后端核心 Service**：重点展示 `CompletableFuture` 的超时编排代码、按标签分组的 `fallbackFortune` 兜底方法逻辑。

为了增加用户体验：
1.前端先保持目前的生成
2.前端直接本地缓存 nickname，avatar。
3.前端 POST /user/login { code, nickname, avatarUrl }

我的页面中的历史记录，要能查看我参与的所有房间结档后的记录（查看当场所有人的数据还是只能看个人？）。要求和房间解散后的，二维折线图和二维表显示内容一致。
因为使用的都是结档时放在room中的all_record数据。同时，要有另外的一个二维折线图，是我的最近多少场次的总积分的折线图
````markdown
# Smart Record 项目风格约束文档

## 1. 产品总气质

Smart Record 不是普通记分小程序，也不是传统设置工具。

整体风格定位为：

> 赛博策略终端 / 玩家数据档案 / 积分复盘工具

核心关键词：

```text
黑底
蓝光
克制
冷静
数据感
终端感
策略感
身份感
复盘感
````

禁止做成：

```text
微信表单页
传统设置页
棋牌工具页
赌博工具页
占卜运势页
儿童化游戏界面
大红大金玄学风
```

产品主线：

```text
房间 = 记录现实
灵感 = 获得策略提示
镜像 = 认识自己的行为画像
我的 = 沉淀玩家身份
```

---

## 2. 全局视觉规范

### 背景

统一黑底：

```css
background: #0A0A0A;
```

可以使用极弱径向光：

```css
background:
  radial-gradient(circle at 20% 0%, rgba(10,132,255,0.12), transparent 32%),
  radial-gradient(circle at 90% 18%, rgba(94,92,230,0.08), transparent 30%),
  #0A0A0A;
```

不要使用大面积纯蓝、大面积渐变、彩色背景。

---

### 主色

```css
--color-primary: #0A84FF;
--color-cyan: #00C8FF;
--color-purple: #5E5CE6;
--color-green: #30D158;
--color-orange: #FF9F0A;
--color-red: #FF453A;
```

蓝色是主高亮色。
绿色只用于“在线 / 成功 / 已连接”。
红色只用于危险、退出、错误，不要大面积使用。

---

### 文字颜色

```css
--text-main: rgba(255,255,255,0.92);
--text-secondary: rgba(255,255,255,0.56);
--text-muted: rgba(255,255,255,0.38);
--text-disabled: rgba(255,255,255,0.24);
```

正文透明度不要低于 `0.40`。
装饰性英文可以低到 `0.28`，但不能影响关键阅读。

---

### 卡片

统一使用暗色玻璃卡，但不要滥用毛玻璃。

主卡：

```css
.primary-card {
  border-radius: 28rpx;
  border: 1rpx solid rgba(10,132,255,0.18);
  background: rgba(255,255,255,0.035);
}
```

次级卡：

```css
.secondary-card {
  border-radius: 24rpx;
  border: 1rpx solid rgba(255,255,255,0.08);
  background: rgba(255,255,255,0.025);
}
```

只允许少数主卡使用：

```css
backdrop-filter: blur(14px);
```

普通卡片不要全部 blur，不要全部 box-shadow。

---

## 3. 赛博风细节

赛博风不是大量英文、不是满屏发光、不是控制台日志堆砌。

应该是：

```text
克制
少字
强层级
精确线框
冷色微光
像终端，不像后台表单
```

允许使用：

```text
细描边
切角按钮
扫描线
HUD小标签
弱英文副标题
数据条
状态点
```

禁止滥用：

```text
大面积发光
全部文字蓝色
全部按钮发光
大量 console 日志
大量英文缩写
大段 AI 废话
```

---

## 4. 中英文使用规则

页面主要面向中文用户。

所有关键功能必须使用中文。

英文只能作为弱装饰、副标题或状态标签。

推荐格式：

```text
房间模式
ROOM MODE
```

```text
人格档案
PERSONA PROFILE
```

中文为主，英文为辅。

英文样式：

```css
.kicker {
  font-size: 18rpx;
  letter-spacing: 4rpx;
  color: rgba(10,132,255,0.72);
}
```

不要出现纯英文主按钮，例如：

```text
CREATE ROOM
CONNECT
STANDARD
TRUSTED
ZERO SUM
```

应该改为：

```text
启动房间
接入房间
标准协议
快速协议
零和结算
```

---

## 5. 按钮规范

### 主按钮

不要用传统大圆角蓝色按钮。

推荐赛博切角或细描边按钮：

```text
启动房间
生成今日策略
分享策略卡
```

主按钮高度建议：

```text
72rpx - 88rpx
```

不要超过 `96rpx`。

按钮不要全屏宽，除非是底部主操作。

---

### 次按钮

用于：

```text
修改类型
重新测试
重新生成
取消
```

样式：

```css
border: 1rpx solid rgba(255,255,255,0.14);
background: rgba(255,255,255,0.02);
color: rgba(255,255,255,0.68);
```

---

### 危险按钮

退出登录、删除类操作：

```css
color: rgba(255,69,58,0.86);
border: 1rpx solid rgba(255,69,58,0.24);
background: transparent;
```

不要大红字单独悬浮。

---

## 6. 页面分工

### 房间页

定位：

```text
记录终端
```

负责：

```text
创建房间
加入房间
积分记录
房间配置
```

风格：

```text
房间初始化系统
接入空间
配置终端
```

不要像普通表单。

创建房间主文案：

```text
启动房间
```

加入房间主文案：

```text
接入房间
扫描接入
房间识别码
```

不要用：

```text
房间号
验证码
```

---

### 灵感页

定位：

```text
策略灵感终端
```

负责：

```text
生成今日策略提示
行动建议
风险提醒
分享策略卡
```

禁止表达为：

```text
塔罗
抽牌
占卜
运势
神谕
预测输赢
```

推荐文案：

```text
校准今日状态
生成你的策略提示
今日策略
行动建议
风险提醒
分享策略卡
```

结果内容必须是：

```text
复盘建议
状态管理
节奏提醒
风险控制
```

不能是：

```text
输赢预测
收益承诺
玄学解释
赌博建议
```

---

### 镜像页

定位：

```text
玩家行为画像
```

负责：

```text
MBTI人格
战绩镜像
人格一致性
分享画像
```

推荐结构：

```text
人格档案
战绩镜像
人格一致性
分享画像
```

避免：

```text
塔罗
黄历
运势
占卜工具矩阵
大量 AI 判读长文
```

镜像页应该回答：

```text
我是一个什么样的玩家？
我的行为数据和人格是否一致？
```

---

### 我的页

定位：

```text
身份终端
```

负责：

```text
玩家身份
总积分
胜率
对局数
人格标签
数据中心
成就
设置入口
```

不要做成传统设置页。

设置应该降级到二级入口：

```text
设置中心
```

首页不要堆满开关。

---

## 7. 敏感词规避

为了小程序审核安全，所有页面、Prompt、fallback、分享图文都必须避开高风险词。

禁止出现：

```text
棋牌
赌博
赌
下注
押注
筹码
牌局
牌桌
打牌
麻将
扑克
德州
梭哈
赢钱
赚钱
发财
稳赚
必胜
翻本
追损
运势
算命
占卜
塔罗
抽牌
神谕
卦象
黄历
风水
开运
转运
改运
预测输赢
胜率提升
```

推荐替换：

```text
牌局 → 对局 / 回合 / 场景
筹码 → 积分
输赢 → 结果波动 / 反馈
运势 → 今日状态 / 今日策略
抽牌 → 生成策略
占卜 → 策略参考 / 状态分析
翻本 → 情绪化修正
```

所有 AI 生成内容必须经过敏感词过滤。
命中敏感词时，丢弃生成结果，使用 fallback。

---

## 8. AI 文案风格

MiMo 或其他 LLM 输出必须遵守：

```text
不预测结果
不承诺收益
不鼓励冒进
不涉及赌博
不使用玄学解释
不使用传统算命口吻
```

推荐语气：

```text
冷静
克制
短句
策略终端感
复盘视角
```

示例安全文案：

```text
今日适合降低试探频率，先明确目标，再进入行动。避免被短期反馈打乱节奏。
```

不要写：

```text
今日运势很好，适合出手。
```

---

## 9. 动效规范

允许：

```text
轻微翻转
轻微浮动
渐入
细线扫描
按钮按压反馈
```

禁止：

```text
持续强发光
大面积粒子
大量 console 打字动画
长时间 loading
滚动时复杂动画
```

加载时间超过 1.5 秒必须给状态反馈。
同类动画必须支持 `reduce-motion`。

---

## 10. 性能规范

必须避免：

```text
每张卡片都 backdrop-filter
每张卡片都 box-shadow
transition: all
onPageScroll 中 setData
大量 DOM 装饰节点
大型弹窗 hidden 而不是 wx:if
```

要求：

```text
弹窗使用 wx:if 懒渲染
页面滚动时不 setData
普通卡片不用 blur
普通卡片不用阴影
setData 不要写入大对象
```

WXSS 禁止：

```css
transition: all;
```

改为：

```css
transition: opacity .2s, transform .2s, background-color .2s;
```

---

## 11. TabBar 图标风格

Tab 图标统一：

```text
线框
圆角
低复杂度
24px 逻辑尺寸
未选中灰白
选中蓝色
少量发光
```

当前四个 Tab：

```text
房间 = 记录终端
灵感 = 策略灵感
镜像 = 玩家画像
我的 = 身份终端
```

图标必须一致，不要一个拟物、一个线框、一个填充风。

---

## 12. 表单设计规范

避免传统表单感。

不要大量使用：

```text
标签 + 双按钮
标签 + 开关
标签 + 输入框
```

应改为：

```text
终端选择卡
配置预览
状态点
HUD标签
分组标题
```

例如创建房间：

不要：

```text
MODE
[FREE FLOW] [ROUND INPUT]
```

应该：

```text
房间模式
ROOM MODE

自由流转
实时记录积分变化

本局录入
每轮结束统一录入
```

---

## 13. 文案长度规范

卡片标题：

```text
2 - 6 字
```

说明文案：

```text
12 - 28 字
```

策略结果 summary：

```text
50 - 90 字
```

镜像判读：

```text
50 字以内
```

不要写长段 AI 解释。

---

## 14. 统一禁用项

全项目禁止：

```text
彩色 Emoji
大红大金玄学风
赌场感视觉
扑克 / 麻将 / 牌桌图形
赚钱暗示
收益承诺
无限抽取
每日运势
占卜口吻
```

---

## 15. 最终设计目标

用户体验应该是：

```text
我在启动一个记录空间
我在获得一条策略提示
我在查看自己的行为画像
我在管理自己的玩家身份
```

而不是：

```text
我在填表
我在算命
我在玩牌
我在看设置
```

所有页面必须服务于：

```text
记录 → 复盘 → 画像 → 身份
```

```
```






# 结算页全面重构（Battle Report）

目标：

将当前结算页

“统计报表”

升级为

“战局报告终端”

统一：

房间页
策略灵感页
镜像页
身份终端页

整体赛博风格。

关键词：

战局复盘
数据终端
人格采集
积分网络
未来科技

禁止：

传统统计图
传统排行榜
后台管理风格
Excel风格矩阵

---

# 一、页面定位重构

当前标题：

结算详情

改为：

BATTLE REPORT

战局复盘

━━━━━━━━━━━━

房间 HGEWRT

结束时间

2026-06-06 02:04

成员

4人

总积分流转

346

━━━━━━━━━━━━

关闭按钮改为：

右上角赛博终端关闭按钮

圆形描边

蓝色呼吸光

不要灰色圆按钮

---

# 二、顶部新增战局总结

放在最顶部

优先级最高

模块：

MISSION SUMMARY

━━━━━━━━━━━━

本局赢家

先天话痨

+143

━━━━━━━━━━━━

最大输家

专业发牌员

-143

━━━━━━━━━━━━

最大单笔

88

━━━━━━━━━━━━

总流转次数

16

━━━━━━━━━━━━

总流转积分

346

━━━━━━━━━━━━

采用：

四宫格终端数据块

数字大字体

蓝色发光

类似控制台状态面板

三、积分趋势图重构

当前问题：

普通折线图

没有未来感

没有战局感

改为：

FLOW TIMELINE

积分演化轨迹

━━━━━━━━━━━━

所有玩家

采用：

霓虹发光曲线

蓝色

绿色

紫色

青色

不同颜色

禁止：

红绿大面积填充

节点增加：

积分事件标记

例如：

+88

+66

-20

直接显示在节点上

支持点击玩家

只查看单人轨迹

曲线下方增加：

时间轴扫描动画

类似雷达扫描


---

# 四、删除总分一览矩阵

当前矩阵：

```text
A→B
+143

B→A
-143

价值极低

占空间

难理解

改为：

SCORE NETWORK

积分关系网络

━━━━━━━━━━━━

玩家头像

作为节点

━━━━━━━━━━━━

玩家之间

用发光线连接

━━━━━━━━━━━━

线条粗细

表示积分往来总量

━━━━━━━━━━━━

点击节点

显示：

向谁输送最多积分

从谁获得最多积分

互动次数

净收益


这部分和镜像页未来人格分析直接关联。

---

# 五、新增战局洞察模块

新增：

BATTLE INSIGHT

━━━━━━━━━━━━

系统自动分析

本局数据

━━━━━━━━━━━━

最高活跃玩家

先天话痨

━━━━━━━━━━━━

最受关注玩家

专业发牌员

━━━━━━━━━━━━

互动中心度

82%

━━━━━━━━━━━━

联盟倾向

高

━━━━━━━━━━━━

竞争强度

中


这些数据全部可以从：

events

直接计算。

无需新表。

六、排行榜重构

当前：

普通排名

改为：

FINAL STANDING

最终排名

━━━━━━━━━━━━

#1

先天话痨

+143

━━━━━━━━━━━━

#2

Happy

0

━━━━━━━━━━━━

#3

专业发牌员

-143

━━━━━━━━━━━━

样式：

终端榜单

不要表格

不要普通列表

前三名：

增加发光徽章

冠军：

蓝色能量环


---

# 七、新增人格数据采集预览

因为这个页面的数据最终会进入镜像系统。

所以新增：

PERSONA SIGNAL

人格信号

━━━━━━━━━━━━

社交活跃度

高

━━━━━━━━━━━━

风险偏好

中

━━━━━━━━━━━━

资源控制欲

高

━━━━━━━━━━━━

联盟倾向

低

━━━━━━━━━━━━

数据已同步镜像系统

SYNCED


让用户知道：

这局牌

正在塑造人格画像。

八、分享海报重构

当前：

分享按钮

价值太低

改为：

生成战局档案

GENERATE REPORT

━━━━━━━━━━━━

生成内容：

玩家昵称

最终排名

积分曲线

人格信号

互动网络

MBTI

镜像称号

━━━━━━━━━━━━

输出：

赛博风战局海报


---

# 九、整体视觉统一

统一整个APP设计语言：

背景：

#05070A

卡片：

#0A0F18

边框：

rgba(0,170,255,0.18)

发光：

rgba(0,170,255,0.35)

主色：

#00AFFF

成功：

#36FF74

危险：

#FF5A5A

文字：

#FFFFFF

次级：

#7C8698

圆角：

16~20

禁止：

纯灰色卡片

纯白按钮

微信风按钮

后台统计风图表

全部采用：

终端
雷达
数据流
网络图
扫描线
能量描边

风格统一到：

策略灵感页 > 身份终端页 > 镜像页 > 房间页 > 结算页

形成同一套 Cyber Terminal Design System。

另外还有一个非常关键的建议：

你现在结算时已经有：

积分记录
积分流向
时间线
成员关系
最终排名

实际上已经够做一个非常强的：

战局关系图
(Battle Relationship Graph)

我会优先砍掉现在的“总分一览矩阵”，直接换成“积分关系网络图”。

因为：

关系网络图
↓
人格画像
↓
策略建议

这才是你整个产品的核心闭环。结算页应该成为房间与镜像之间的桥梁，而不是单纯的统计页面。