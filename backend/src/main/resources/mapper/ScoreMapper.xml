<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mahjong.score.mapper.ScoreMapper">

    <insert id="insertBatch">
        INSERT INTO score (id, session_id, room_id, round_no, user_id, score, created_by, created_at)
        VALUES
        <foreach collection="list" item="s" separator=",">
            (#{s.id}, #{s.sessionId}, #{s.roomId}, #{s.roundNo}, #{s.userId}, #{s.score}, #{s.createdBy}, #{s.createdAt})
        </foreach>
    </insert>

</mapper>
